# Reports Status

`reports/model_selection/` is the current source of truth for model metrics,
leaderboards, runtime, model cards, and final operational champion selection.

Legacy artifacts retained only for traceability under `reports/archive/`:

- `reports/archive/model_comparison_report.md`
- `reports/archive/model_comparison_metrics.csv`

These legacy files were generated before the corrected baseline definitions,
the operational/oracle split, and champion registry export. They must not be
used as the final project conclusion.
